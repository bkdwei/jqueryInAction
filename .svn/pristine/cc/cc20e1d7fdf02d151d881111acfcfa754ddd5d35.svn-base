//定义模块
var select = (function () {

    return {
        getHeadOne: function () {
            var headOne = $(".header-one");
            c.log("headOne is " + headOne.html());
        },
        getLogin: function () {
            var login = $("#login");
            c.log("login is " + login.html());
        },
        getImg: function () {
            var image = $("img");
            c.log("img is " + image.attr("src"));
            c.log("img is " + image.attr("width"));
            c.log("img is " + image.attr("height"));
        },
        getInput: function () {
            var input = $("input:eq(1)");
            var input1 = $("input:first");
            var button1 = $("button:contains('登录')");
            var button2 = $(":button");
            c.log("input is " + input.attr("class"));
            c.log("input is " + input1.attr("class"));
            c.log("button1 is " + button1.attr("class"));
            c.log("button2 is " + button2.text());
        },
        getHeadOne1: function () {
            var headOne1 = $("span[class='header-one']");
            c.log("headOne is " + headOne1.html());
        },
    };
})();
